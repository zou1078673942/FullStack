// java 强类型语言 
// ts 为 js 的超集                   该函数的返回值一定为number
// var getArea = (w: number, h: number): number => {
//     return w * h
// }
// console.log(getArea(1.11,2))
var getArea = function (w, h) {
    return w * h;
};
console.log(getArea(1.11, 2));
