// import * as actionTypes from './constant';

const defaultState = {
  recommendList: []
}
export default (state = defaultState, action) => {
  switch(action.type) {
    default:
        return state
  }
}